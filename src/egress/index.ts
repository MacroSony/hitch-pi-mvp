export { DEFAULT_EGRESS_LIMITS, EgressClient, EgressError } from "./client.js";
export type {
  AddressResolver,
  EgressClientOptions,
  EgressErrorCode,
  EgressMethod,
  EgressRequest,
  EgressResponse,
  EgressServiceConfig,
} from "./client.js";
